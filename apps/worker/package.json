{
  "name": "@vada/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "echo \"worker lint pending\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vada/config": "workspace:*",
    "@vada/contracts": "workspace:*",
    "@vada/db": "workspace:*",
    "bullmq": "^5.8.0",
    "ioredis": "^5.4.1",
    "zod": "^3.23.8"
  }
}